# Backend environment configuration

# Server
PORT=8000
HOST=0.0.0.0

# CORS (comma-separated origins)
CORS_ORIGINS=https://softdab.tech,https://www.softdab.tech

# Email sender identity
FROM_EMAIL=info@softdab.tech
FROM_NAME=SoftDAB

# Resend (preferred provider)
# RESEND_API_KEY=your_resend_api_key

# SMTP fallback (or primary if Resend not configured)
SMTP_HOST=smtp.zoho.eu
SMTP_PORT=587
SMTP_USER=noreply@softdab.tech
SMTP_PASS=your_smtp_password
SMTP_TLS=true

# Timezone for admin/API date formatting
TIMEZONE=Europe/Kyiv

# Database directory (optional; defaults to /var/www/softdab/backend/data)
# DB_DIR=/var/www/softdab/backend/data
